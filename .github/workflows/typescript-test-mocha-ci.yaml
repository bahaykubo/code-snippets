name: Typescript - Test - Mocha

on:
  workflow_call:

jobs:
  changes:
    uses: ./.github/workflows/check-for-changes.yaml
    with:
      filters: |
        requires-check:
          - 'typescript-test-mocha/**'
          - '.github/workflows/typescript-test-mocha-ci.yaml'

  test:
    needs: changes
    if: needs.changes.outputs.requires-check == 'true'
    defaults:
      run:
        working-directory: ./typescript-test-mocha
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repo
        uses: actions/checkout@v7

      - name: Use Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install npm packages
        run: npm run ci

      - name: Lint project
        if: always()
        run: npm run lint

      - name: Start kafka
        run: ./scripts/docker/up.sh

      - name: Run tests
        if: always()
        run: npm test

      - name: Stop kafka
        if: always()
        run: ./scripts/docker/down.sh
