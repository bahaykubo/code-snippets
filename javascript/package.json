{
  "name": "code-snippets-javascript",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "ci": "rm -rf node_modules && yarn install --frozen-lockfile",
    "lint": "eslint . && yarn prettier --check .",
    "lint-fix": "eslint . --fix && yarn prettier --write . && yarn lint",
    "test": "mocha",
    "test-watch-file": "yarn test --watch ",
    "test-watch": "yarn test-watch-file 'snippets'",
    "watch-file": "yarn run nodemon"
  },
  "devDependencies": {
    "@babel/core": "^7.23.2",
    "@babel/preset-env": "^7.23.2",
    "@babel/register": "^7.22.15",
    "@types/chai": "^4.3.9",
    "@types/mocha": "^10.0.3",
    "@types/node": "^20.8.10",
    "babel-plugin-module-resolver": "^5.0.2",
    "chai": "^4.3.10",
    "dayjs": "^1.11.10",
    "eslint-plugin-mocha": "^10.2.0",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "nodemon": "^3.0.1",
    "style-lint-js": "^1.1.0"
  }
}
