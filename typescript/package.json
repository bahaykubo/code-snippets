{
  "name": "code-snippets-typescript",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "ci": "rm -rf node_modules && yarn install --frozen-lockfile",
    "lint": "eslint snippets test-jest test-mocha test-jasmine",
    "prettier-write": "prettier --write snippets test-jest test-mocha test-jasmine",
    "lint-fix-pretty": "eslint snippets test-jest test-mocha test-jasmine && yarn lint && yarn prettier-write",
    "watch": "yarn run nodemon"
  },
  "devDependencies": {
    "@types/node": "^20.8.10",
    "date-fns": "^2.30.0",
    "date-fns-tz": "^2.0.0",
    "moment": "^2.29.4",
    "nodemon": "^3.0.1",
    "style-lint-js": "^1.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "xml-formatter": "^3.6.0"
  }
}
