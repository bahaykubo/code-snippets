{
  "name": "code-snippets-typescript",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "ci": "rm -rf node_modules && yarn install --frozen-lockfile",
    "lint": "eslint && yarn prettier --check .",
    "lint-fix": "eslint --fix && yarn prettier --write . && yarn lint",
    "watch": "yarn run nodemon"
  },
  "devDependencies": {
    "@types/node": "^20.8.10",
    "date-fns": "^2.30.0",
    "date-fns-tz": "^2.0.0",
    "moment": "^2.29.4",
    "nodemon": "^3.0.1",
    "style-lint-js": "^1.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "xml-formatter": "^3.6.0"
  }
}
